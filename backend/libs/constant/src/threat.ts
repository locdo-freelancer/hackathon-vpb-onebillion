export enum ThreatSeverity {
  CRITICAL = "critical",
  HIGH = "high",
  MEDIUM = "medium",
  LOW = "low",
}

export enum ThreatType {
  IP = "ip",
  DOMAIN = "domain",
  URL = "url",
  HASH = "hash",
}

export enum ThreatStatus {
  ACTIVE = "active",
  BLOCKED = "blocked",
  FLAGGED = "flagged",
  MONITORING = "monitoring",
}
